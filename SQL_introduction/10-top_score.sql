-- Script to list all records of the table second_table in the database hbtn_0c_0
-- The query retrieves all records from the second_table, ordering them by score in descending order.
SELECT score, name FROM second_table
ORDER BY score DESC;
