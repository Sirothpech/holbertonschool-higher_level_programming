-- Script to compute the score average of all records in the table second_table
-- The query uses the SELECT statement with the AVG() function to calculate the average of the scores.
SELECT AVG(score) AS average
FROM second_table;
