-- Script to update the score of Bob to 10 in the table second_table
-- The query uses the UPDATE statement to modify the score for the row where name='Bob'.
UPDATE second_table
SET score = 10
WHERE name = 'Bob';
