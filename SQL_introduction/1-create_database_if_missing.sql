-- Script to create the database hbtn_0c_0 in MySQL server if it doesn't exist.
-- The 'CREATE DATABASE IF NOT EXISTS' statement creates the database only if it's not already present.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
